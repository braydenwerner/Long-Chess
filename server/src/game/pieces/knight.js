class Knight {
    constructor(color) {
        this.color = color;
    }

    //if not out of bounds, can jump 8 directions
    getMoves(board, row, col) {
        if (this.color === "White") {

        } else {

        }
    }
}

module.exports = Knight;